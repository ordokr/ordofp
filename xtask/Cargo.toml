[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
description = "Local verification driver: cargo run -p xtask -- <gate|deny|wasm|miri|fuzz-smoke|all|deep>"

[lints]
workspace = true
