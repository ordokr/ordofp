[package]
name = "xtask"
version = "0.1.2"
edition = "2024"
publish = false
license = "Apache-2.0"
description = "Local verification driver: cargo run -p xtask -- <gate|stable|deny|wasm|miri|fuzz-smoke|perf-guard|pgo|semver|all|maint|deep>"

[lints]
workspace = true
