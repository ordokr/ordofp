target
Cargo.lock
# Backup files generated by rustfmt
**/*.rs.bk
*.iml
benches-control
benches-variable

# Editor and local tool state
.vscode/
.idea
.worktrees/

# Windows device name artifacts
nul
NUL

# Build artifacts
*.exe
*.pdb
*.rlib
*.log

# Stray dirs created if cargo is ever run with a misconfigured `--target true`
true/
**/true/

# Machine-specific cargo config (MSVC linker/SDK paths differ per machine).
# Copy .cargo/config.toml.example and adjust paths for your toolchain.
.cargo/config.toml
